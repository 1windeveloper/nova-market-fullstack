spring:
  datasource:
    url: jdbc:h2:mem:market_files_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: market-files-test
  public-url: http://localhost:9000/market-files-test
  create-bucket: false
